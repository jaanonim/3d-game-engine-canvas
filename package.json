{
  "name": "3d-game-engine-canvas",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run doc && npm publish",
    "doc": "typedoc --exclude ./src/main.ts --entryPointStrategy expand ./src"
  },
  "devDependencies": {
    "typedoc": "^0.23.15",
    "typescript": "^4.6.4",
    "vite": "^3.1.0"
  }
}
